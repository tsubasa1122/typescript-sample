const person: {
  name: string;
  age: number;
  role: [number, string];
} = {
  name: 'nakano',
  age: 30,
  role: [2, 'author']
};

// person.role.push("admin");
// person.role[1] = 10;

// person.role = [0, "admin", "string"];
